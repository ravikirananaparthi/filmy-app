/**
 * /upload/pick — Custom gallery picker built on expo-media-library.
 * 3-column photo grid with multi-select (up to 10) and "Next →" header button.
 */
import PickScreen from '@screens/upload/PickScreen';
export default PickScreen;
